import { combineReducers } from 'redux'
import AppReducer from '../../../Pages/App/Redux/Reducers'

// Here import then add you reducers
const rootReducer = combineReducers({
    AppReducer
})

export default rootReducer
