import React from 'react';

import { ToastContainer as LocalToastContainer } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

const ToastContainer = () => {
    return (
        <LocalToastContainer />
    );
}

export default ToastContainer;